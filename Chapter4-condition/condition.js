/**
 * a == b => 判斷 a, b 是否相等
 * a != b => 判斷 a, b 是否不相等
 * ! => 將前面的 boolean value 反轉
 * a && b => a 且 b
 * a || b => a 或 b
 * a > b
 * a < b
 * a >= b
 * a <= b
 */

let a = 1;
let b = 1;
console.log(a == b);
console.log(!(a == b))